Trained Big Five clustering artifacts will be written here.

Expected generated files:

```text
api/ml/artifacts/bigfive/scaler.joblib
api/ml/artifacts/bigfive/kmeans.joblib
api/ml/artifacts/bigfive/clusters.json
```

These files are generated by training and should stay out of git.
